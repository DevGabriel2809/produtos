const header=document.querySelector('[data-header]');const menu=document.querySelector('[data-menu]');const nav=document.querySelector('[data-nav]');window.addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>20),{passive:true});menu?.addEventListener('click',()=>{menu.classList.toggle('open');nav.classList.toggle('open')});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{menu.classList.remove('open');nav.classList.remove('open')}));const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');obs.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));function count(el){const n=+el.dataset.count;const suffix=el.dataset.suffix||'';let start;function step(t){start??=t;const p=Math.min((t-start)/1300,1);el.textContent=Math.round(n*(1-Math.pow(1-p,3))).toLocaleString('pt-BR')+suffix;if(p<1)requestAnimationFrame(step)}requestAnimationFrame(step)}const nums=document.querySelectorAll('[data-count]');const numObs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){count(e.target);numObs.unobserve(e.target)}}),{threshold:.6});nums.forEach(n=>numObs.observe(n));document.querySelector('[data-form]')?.addEventListener('submit',e=>{e.preventDefault();const b=e.target.querySelector('button');const old=b.textContent;b.textContent='Solicitação enviada';setTimeout(()=>b.textContent=old,2600);e.target.reset()});
